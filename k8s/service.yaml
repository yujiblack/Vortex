apiVersion: v1
kind: Service
metadata:
  name: voxdeploy
  namespace: voxdeploy
spec:
  selector:
    app: voxdeploy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
